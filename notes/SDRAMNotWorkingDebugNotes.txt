* things to try

try resetting every 5s to see if autorefresh commands work

put pulldown on cke / pull up on dqm so that when power is applied, they in the right states according to the "Power up Sequence" section


* not refreshing

starts reading bad values @ ~5 minutes
    1 bit incorrect from 16 different values

can't read/write @ 






* refreshing

starts reading bad values @ ~X minutes

can't read/write @ 
